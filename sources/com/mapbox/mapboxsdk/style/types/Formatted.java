package com.mapbox.mapboxsdk.style.types;

import X.AnonymousClass00U;
import java.util.Arrays;

public class Formatted {
    public int hashCode() {
        return Arrays.hashCode((Object[]) null);
    }

    public boolean equals(Object obj) {
        if (this == obj) {
            return true;
        }
        if (obj == null || getClass() != obj.getClass()) {
            return false;
        }
        return Arrays.equals((Object[]) null, (Object[]) null);
    }

    public String toString() {
        return AnonymousClass00U.A0M("Formatted{formattedSections=", Arrays.toString((Object[]) null), '}');
    }
}
